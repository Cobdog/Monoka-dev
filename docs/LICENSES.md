# Third-party license inventory

> **What this file is:** the point-in-time, machine-checkable inventory of every
> third-party component this project consumes — code dependencies, vendored
> packs, user-fetch-only components, and model weights — with the SPDX verdict,
> how we consume it, and the obligations it triggers. It is the ONE
> consolidated third-party notices file of the distribution surface (directive
> `c5673267`: heavy diligence, minimal surface).
> **Why a separate file:** [PROVENANCE.md](PROVENANCE.md) records who *we* are
> licensing-wise (AGPLv3 rationale, fork lineage, distribution policy);
> LICENSES.md records what *we ship*; [licenses/registry.md](licenses/registry.md)
> is the deep diligence record (per-component verdicts, obligations, C&D blast
> radius) and [licenses/policy.md](licenses/policy.md) the decision rules,
> AGPLv3 compliance map, and maintenance charter. Cross-linked so any of them
> is a valid entry point.
>
> **Regeneration:** `pnpm license:audit` (gate + CI) re-derives §1 from
> `node_modules` manifests, re-checks that every vendored pack ships its LICENSE
> file, enforces the §3 license gate (never vendor what we can't ship), and
> verifies registry↔reality lockstep (every dep / fetch-catalog id / pack id /
> shipped directory named in licenses/registry.md). After dependency changes,
> paste its table output back into §1.

**Verification legend** — `[API-2026-09-14]` checked against the GitHub /
Hugging Face API on 2026-09-14 (task 68rnn84); `[LOCAL]` verified in this
repo's tree; `[DOC]` recorded from research docs (`docs/research/*`), not
independently re-verified this pass.

**Consumption classes:**

| Class | Meaning | License gate |
| --- | --- | --- |
| vendored | Code ships inside this repo (`vendor/nodes/`), pinned revision | permissive only (Apache-2.0/MIT/ISC-class) |
| user-fetch | The studio installs it into the user's own ComfyUI checkout: from a local copy the user nominates, or through the consent-gated network fetcher (task hgjbea2) | any — license surfaced at consent time; NO-LICENSE and GPL live here forever |
| documented-only | Referenced in research docs as pattern/idea source; no code or asset flows into the repo | any — cite, never copy |
| weights | Model tensors; never in the repo, never copied — linked into the user's model roots | any — surfaced at install time |

## 1. Runtime code dependencies (39 direct; MIT 31, Apache-2.0 3, ISC 4, MIT+1 vitest)

> **Dated note (2026-09-21, task nwtoz6y):** `vitest` 5.0.1 (MIT) added to the
> table — the 39th direct dep was shipping without a §1 row (drift found by the
> new registry-lockstep audit check; all permissive, conclusion unchanged).
>
> **Dated note (2026-09-19, task ukyxwfa):** `pino-pretty` 13.1.3 (MIT) added
> as a dev dependency — the start.sh launcher pipes dev-mode server logs
> through it behind `MINIMAX_LOG_PRETTY=1` (never a runtime requirement; the
> logger degrades honestly when it is absent).
>
> **Dated note (2026-09-18, task rrxlw2r):** table regenerated from
> `pnpm license:audit` against `node_modules` manifests — the nine additions
> of 2026-09-17 are now in the table (fulfilling that day's regeneration
> note), plus refreshed versions (`@eslint/js`/`eslint` 9.19.5 → 9.39.5).
> All permissive and AGPLv3-compatible; the conclusion is unchanged.

Single workspace (`pnpm-workspace.yaml` lists only the root). Every direct
dependency is permissive and AGPLv3-compatible; obligations are notice
retention only. Table regenerated by `pnpm license:audit`:

| Component | Version | SPDX | Class | Obligations | Status |
| --- | --- | --- | --- | --- | --- |
| @base-ui/react | 1.8.0 | MIT | runtime dep | notice | ok |
| @huggingface/transformers | 4.3.0 | Apache-2.0 | runtime dep | notice | ok |
| better-sqlite3 | 13.0.3 | MIT | runtime dep | notice | ok |
| d3-selection | 3.0.0 | ISC | runtime dep (canvas pan/zoom) | notice + copyright | ok |
| d3-transition | 3.0.1 | ISC | runtime dep (canvas pan/zoom) | notice + copyright | ok |
| d3-zoom | 3.0.0 | ISC | runtime dep (canvas pan/zoom) | notice + copyright | ok |
| hash-wasm | 4.12.0 | MIT | runtime dep | notice | ok |
| lucide-react | 0.468.0 | ISC | runtime dep | notice + copyright | ok |
| pino | 10.3.1 | MIT | runtime dep | notice | ok |
| react | 19.3.0 | MIT | runtime dep | notice | ok |
| react-dom | 19.3.0 | MIT | runtime dep | notice | ok |
| react-rnd | 10.5.3 | MIT | runtime dep (floating docks) | notice | ok |
| three | 0.186.0 | MIT | runtime dep (pose-rig viewport — lazy `?poserig=1` chunk only) | notice | ok |
| ws | 8.21.3 | MIT | runtime dep | notice | ok |
| zustand | 5.0.15 | MIT | runtime dep | notice | ok |
| @eslint/js | 9.39.5 | MIT | dev dep | — (not distributed) | ok |
| @playwright/test | 1.63.0 | Apache-2.0 | dev dep | — | ok |
| @rollup/rollup-win32-x64-msvc | 4.63.1 | MIT | dev dep (Windows binary) | notice | ok |
| @types/better-sqlite3 | 9.6.0 | MIT | dev dep | — | ok |
| @types/d3-selection | 3.0.12 | MIT | dev dep | — | ok |
| @types/d3-transition | 3.0.9 | MIT | dev dep | — | ok |
| @types/d3-zoom | 3.0.8 | MIT | dev dep | — | ok |
| @types/node | 22.20.1 | MIT | dev dep | — | ok |
| @types/react | 19.3.0 | MIT | dev dep | — | ok |
| @types/react-dom | 19.3.0 | MIT | dev dep | — | ok |
| @types/three | 0.186.0 | MIT | dev dep | — | ok |
| @types/ws | 8.18.1 | MIT | dev dep | — | ok |
| @vitejs/plugin-react | 4.7.0 | MIT | dev dep | — | ok |
| eslint | 9.39.5 | MIT | dev dep | — | ok |
| eslint-plugin-react-hooks | 5.2.0 | MIT | dev dep | — | ok |
| eslint-plugin-react-refresh | 0.4.26 | MIT | dev dep | — | ok |
| globals | 15.15.0 | MIT | dev dep | — | ok |
| pino-pretty | 13.1.3 | MIT | dev dep (launcher pretty logs, dev mode only) | — | ok |
| stylelint | 17.15.0 | MIT | dev dep | — | ok |
| stylelint-config-standard | 40.0.0 | MIT | dev dep | — | ok |
| typescript | 5.9.3 | Apache-2.0 | dev dep | — | ok |
| typescript-eslint | 8.70.0 | MIT | dev dep | — | ok |
| vite | 6.4.3 | MIT | dev dep | — | ok |
| vitest | 5.0.1 | MIT | dev dep (the unit-suite runner) | — | ok |

**Notice satisfaction:** we distribute source-from-repo, so per-package notices
ride in `node_modules/<pkg>/package.json` (`license` fields) and the pnpm
lockfile pins exact versions. If a bundled/binary distribution ever happens, a
third-party notices bundle must be generated first (§9 follow-up 2).

## 2. Vendored code (ships in this repo)

| Component | Version / pin | SPDX | How consumed | Obligations | Status |
| --- | --- | --- |---|---|---|
| ComfyUI-VDN-H3 (Saganaki22) | `3eb63496c24ca70faaf8a14b6c75fcb480e34bf1` (2026-09-12, v1.5.2) | Apache-2.0 `[API-2026-09-14]` `[LOCAL]` | vendored: `vendor/nodes/ComfyUI-VDN-H3/`, installed by copying code + LINKING weights into the managed checkout | Apache-2.0 §4: retain LICENSE (✓ shipped in tree, checked by `pnpm license:audit`), state changes (functional content verbatim; excluded `.git/`, `.github/`, `assets/` demo videos, workflow PNGs — recorded in PROVENANCE) | clean |
| LongCache installer logic (from the maintainer's ComfyUI-VDN-H3-24GB fork, `tools/install_minimax_block_loop_hook.py`) | fork @ local scratchpad (not a published pin) | Apache-2.0 (fork's stated license) — see §8 for the GPL-3.0 second reading | vendored as a **verbatim port of the patch-content constants + discipline** inside `server/enginePatch.ts` (TS); applies only at runtime, user-local, consent-gated | attribution (in-file, present) + §8 analysis | clean, see §8 |
| bruxosdovfx Camera H3 compiler (NyckM/3d-Camera-control-H3-Minimax) | `846880de859959e801b2c506dc424bd5c8b5c6c4` (2026-09-13, README v19.1) | Apache-2.0 `[LOCAL]` (LICENSE read from the fresh clone before porting, task ving89w) | ported into `src/lib/camera/` as a pure, UI-free TS library (task ving89w) — a faithful port of camera.py / trajectory_math.py / motion.py / diagnostics.py (compiler half only; the ComfyUI node, web editor, banner and experimental prompt modes are NOT ported), with byte-parity goldens generated by the upstream Python itself (scripts/fixtures/camera-goldens.json + camera-goldens.py) | Apache-2.0 §4: attribution (per-file provenance headers citing repo + commit, present) + state changes (language port; quirks preserved as quirks, deviations documented in the module headers) — a port, not a copy: no upstream bytes ship | clean |

The vendored VDN pack's *weights* (~4.3 GB VDN checkpoints) are NOT vendored —
they are user-fetched from Hugging Face and land as links (never copies) in the
user's model roots (§5).

### 2b. First-party custom node (our code — task k271ykk)

| Component | Version / pin | SPDX | How consumed | Obligations | Status |
| --- | --- | --- | --- | --- | --- |
| `custom-nodes/minimax-lora-form-adapter/` (MiniMax-H3 LoRA Form Adapter) | `v1.0.0` (the node's own version) | **MIT** (LICENSE file in the pack) | first-class repo module; installs into a managed/external checkout from the studio's own payload (`installMode: 'first-party'`, no network) or via the consent-gated local-install fetch-catalog entry; independently releasable (copy the directory = the pack) | MIT notice (shipped) | clean |

Licensing-aware asset posture (the §5-class question resolved conservatively,
per docs/research/h3-lora-form-compatibility.md §5.6): the node ships **zero
MiniMax-derived bytes** — the projection encoder is derived at first use from
the user's own local artifacts (the model's live `adaln_t_table` + larryvrh's
Apache-2.0 E-grid when installed, or a grid built by the pack's
`tools/derive_projection.py` from the user's own full-width checkpoint).
Precomputed matrices are a documented advanced option whose generation and
posture the user owns. `pnpm license:audit` checks that first-party packs
ship a LICENSE and stay permissive.

**Test-only derived vectors** (small deterministic slices, ~740 KB, provenance
in `custom-nodes/minimax-lora-form-adapter/tests/fixtures/FIXTURES.md`): the
two canonical `adaln_t_table` grids, 64 columns of the Apache-2.0 E-grid,
block-0 adaln tensors of the public larryvrh Turbo v4 LoRA, and block-0
tensors of kijai's public Acc full/pruned pair. These are MiniMax-derived
bytes committed for offline golden tests (explicitly sanctioned by the task —
the alternative, a network-dependent golden, would silently skip in CI); they
are not loadable model assets and never leave the test fixture.

## 3. User-fetch-only components (never vendored)

The installable subset (Larryvrh, T8mars) is tracked as data in
`server/engineNodes.ts` (`ENGINE_NODE_PACKS`), surfaced in Settings → Node
packs with the SPDX badge visible at consent time, and fetchable through the
consent-gated local-first fetcher (task hgjbea2: `server/fetchCatalog.ts` +
`server/fetcher.ts`, Settings → Fetchable items) — the GPL items are
deliberately **fetchable-but-flagged**: the license text is surfaced at
consent time, we redistribute nothing, vendoring stays forbidden. The
license gate is enforced mechanically: `pnpm license:audit` fails the build
if any non-permissive entry is `installMode: 'vendor'`. LanPaint is not an
installable component — its GPL status confines it to pattern-adoption in
our own code.

| Component | Pin | SPDX | Why user-fetch | Obligations | Status |
| --- | --- | --- | --- | --- | --- |
| ~~facok/comfyui-krea2-controlnet~~ | `main` (branch) | **NO-LICENSE** `[API-2026-09-14]` | **RETIRED 2026-09-26** — the registry row + fetch entry were cut (wiring-check §1.5: no builder ever emitted the classes); kept as license history | none — never redistributed, now not installable | retired |
| T8mars/comfyui-minimax-h3-audio-T8 | `main` (branch — resolved + stamped at fetch time) | **GPL-3.0-or-later** `[API-2026-09-14]` (LICENSE file is an SPDX-notice, not full text) | policy: GPL packs are never vendored even though GPL-3.0 ↔ AGPLv3 are combining-compatible — vendoring would fold third-party GPL code into our distribution and couple our releases to an unmaintained-by-us contributor set; fetching the user their own copy triggers none of that | none triggered (we never redistribute); if we ever vendored: full GPL §4–§6 duties + combined-work terms | fetchable-but-flagged through the consent flow; pattern-adopt remains the rule for our own code; gate holds |
| Larryvrh/ComfyUI-MiniMax-H3-Turbo | `4274783a23afcfdbea3b4876cb79effd6c510785` | Apache-2.0 `[API-2026-09-14]` | **not a license reason** — simply not vendored yet; user-fetch from a local copy or the fetcher until the vendoring increment | none beyond notices when vendored | candidate for vendoring |
| lbouaraba/comfyui-krea2edit | `86f886dac23013d88996e3a2e99093ba44d322fb` | Apache-2.0 `[API-2026-09-14]` (LICENSE file + GitHub license record) | permissive, but not vendored (task t8u00uu ships no third-party code; same posture as Larryvrh) — the Identity Edit dual-conditioning node pack behind the Instruct/removal/two-ref edit families | notice when vendored | fetchable; pinned by SHA (solo-maintained, v2 retrain in progress — re-verify at v2) |
| alexw5702-afk/krea2-anypaint | `675be5a91eadbf8b8997b21c0e8e1848310b9571` | MIT `[API-2026-09-14]` (LICENSE file; NOTICE credits Rebels + ostris) | same as above — the AnyPaint mask nodes behind the refine/outpaint edit families | notice when vendored | fetchable |
| scraed/LanPaint | — | **GPL-3.0** `[API-2026-09-14]` | same GPL policy as T8mars; deferred on quality/cost grounds anyway (krea2-edit-mode.md §5) | none while not distributed | watch tier |
| Lightricks/ComfyUI-LTXVideo | `15d09abb5a187a8dcaea2fc31fe51ee96e6c9d0d` `[LOCAL-2026-09-15]` | **LTX-2-Community-License** (LICENSE file read from a fresh clone — the same agreement as the LTX-2.3 weights) | custom permissive-with-conditions license, not SPDX-listed: never vendored; the IC-LoRA guide machinery (LTXICLoRALoaderModelOnly / LTXAddVideoICLoRAGuide / LTXVSetAudioRefTokens / LTXVTiledVAEDecode / LTXFloatToInt) behind the LTX-2.3 utility family (task 068xwy3) — the node INPUT schemas ported in src/lib/graph/ltx23.ts were verified against this exact revision | none triggered (user-fetch, we redistribute nothing) | fetchable; pinned by SHA |
| kijai/ComfyUI-KJNodes | `d3cfe21625e5170126ce06fbfcfe1d88108688c3` `[LOCAL-2026-09-15]` (full LICENSE text read from a fresh clone) | **GPL-3.0** | same GPL policy as T8mars: never vendored, fetchable-but-flagged; the LTX-2.3 utilities use three nodes (GetImageSizeAndCount, ImagePadKJ, VAELoaderKJ) | none triggered (we never redistribute) | fetchable-but-flagged |
| fxtdstudios/radiance | `64fee4144cd4818a087b4269effe6c40d6f6fe2f` `[API-2026-09-15]` | **GPL-3.0** | same GPL policy; one node used — Float32ColorCorrect, generation-load-bearing in the official LTX-2.3 outpaint template (the gamma-2 padded-guide correction) | none triggered (we never redistribute) | fetchable-but-flagged |
| supElement/ComfyUI_MinimaxH3_AutoContext | `f1062d34e3c25ef421b2aadeb69f2d21831d1625` `[API-2026-09-16]` | **Apache-2.0** (LICENSE file, fresh fetch) | not a license reason — permissive, simply not vendored yet; segmented-inference pack, deep-read committed | notice when vendored | fetchable; **candidate for vendoring** (Apache-2.0, 14 files, no weights) |
| ComfyUI (comfyanonymous — the fetcher's engine-checkout entry) | tag `v0.34.0` → `12d5279438bfefc058a269eae805ceab6047777f` `[API-2026-09-14]` | **GPL-3.0** | the reference checkout for the managed runtime's clone-on-demand seam: fetched onto the user's disk by consent, never vendored, never conveyed by us (the §8 analysis, applied to the whole engine instead of one file) | none triggered (we never redistribute ComfyUI); the checkout is the user's own working copy | fetchable-but-flagged; gate holds |

## 4. Documented-only / cited components (no code or asset flows)

| Component | SPDX | Where cited | Rule |
| --- | --- | --- | --- |
| malcolmrey h3-center docs (HF dataset `malcolmrey/various`) | **WTFPL** `[API-2026-09-14]` | transitions/RefMod research | cite freely; WTFPL permits everything, but keep citation-not-copy as the habit for guides |
| GENKAIx/Genkai-ComfyUI-Nodes (PromptSync) | **NO-LICENSE** `[API-2026-09-14]` | node ecosystem sweep §1 | pattern-adopt, code-do-not-copy; MIT/Apache request to author is the cheap unlock |
| tori29umai0123/ComfyUI-MiniMaxH3-SingleFrame | **NO-LICENSE** `[API-2026-09-14]` | node ecosystem sweep §2 | idea only (temporal-RoPE alignment) |
| shootthesound/ComfyUI-H3Studio | **NO LICENSE file** (README carries an MIT badge — badge without text is not a grant) `[API-2026-09-14]` | node ecosystem sweep §2 | watch |
| karuvanan/MiniMax-H3-Director-Cut-Studio | **MIT** LICENSE file present; GitHub classifies NOASSERTION `[API-2026-09-14]` — *correction: the sweep doc's "license unclear" is stale* | competitive scan | watch |
| thedoxen/Krea-2-pose-controlnet | Apache-2.0 per research docs `[DOC]`; HF repo shows no license tag — unverified | krea2-edit-mode.md §2 | verify before any adoption |
| LongMedia latent_ops, FL-MiniMaxH3, AIMixer Director (Apache-2.0); scottmudge HybridLoader, Continuum N2B (MIT) | per research docs `[DOC]` | node ecosystem sweep §4 shortlist | vendor/fetch only after re-verifying license at adoption time |

## 5. Model weights & assets (never in the repo)

Weights are downloaded by the user from Hugging Face and **linked** into their
model roots (symlink → junction → hardlink → refuse; never a byte-copy — the
`linkNeverCopy()` invariant in `server/engineNodes.ts`).

| Asset | License | Class | Duties that ride with use | Status |
| --- | --- | --- | --- | --- |
| MiniMax H3 base / turbo / audio-VAE weights (MiniMax-AI, gated HF) | MiniMax H3 Community License `[DOC]` (region + commercial terms confirmed in T8mars & Motion-Context READMEs, Comfy docs) | weights | community-reported region exclusions (EU/UK/KR/US) and commercial use via a MiniMax license sold through Comfy; outputs follow the same terms — surfaced in-app by the one-time LicenseNotice | surfaced |
| OpenVDN/vdn-minimax-h3 (VDN branch checkpoints) | `minimax-h3-community-license-agreement` `[API-2026-09-14]` | weights (fetched by the vendored VDN pack's own download path) | same class as above | surfaced |
| Viggle/Viggle-Animate | `minimax-h3-community-license` `[API-2026-09-14]` | weights (H3-ref2VA finetune — watch tier) | same class | documented |
| t8star/Vdn-Minimax-H3-Comfy, FastVideo FastH3, Tutu 20→8 NFE LoRA | MiniMax Community License `[DOC]` | weights (watch tier) | same class | documented |
| MiniMax-H3-Fun-Controlnet-Union (alibaba-pai) | MiniMax H3 Community License `[DOC]` | weights (planned first-class) | same class; usage guidance (guidance=1.0 etc.) tracked in fun-control-input-surface.md | documented |
| Krea 2 base + style-reference module | Krea AI Community License `[DOC]` | weights | Krea community terms | documented |
| conradlocke/krea2-identity-edit (Identity Edit v1.2 LoRA + _r128/_r64 cuts) | `krea-2-community-license` `[API-2026-09-14]` (repo LICENSE.pdf + NOTICE; sha-pinned in the fetch catalog, task t8u00uu) | weights (fetch catalog — the Instruct/removal/two-ref edit families) | derivative works under same license; commercial only <$1 M/yr revenue; moderation + AI-disclosure duties; author additionally disallows non-consensual use of real people (SFW-only training) — surfaced at consent time in the fetcher | surfaced |
| yijunwang2/krea2-anypaint (AnyPaint rank-32 adapter) | `krea-2-community-license` `[API-2026-09-14]` (repo LICENSE.pdf; its pipeline code carries a separate PIPELINE_LICENSE — only the adapter weights are fetched) | weights (fetch catalog — the refine/outpaint edit families) | same duties as above; training data not disclosed (unofficial) — surfaced at consent time | surfaced |
| LTX-2.5 / ACE-Step 1.5 / Z-Image weights | per their HF cards — verify at first-classing time `[DOC]` | weights | none recorded yet — §9.3 | open |
| LTX-2.3 utility weights: Lightricks/LTX-2.3 (+ `-fp8`) checkpoints, spatial-upscaler x2-1.1, distilled-384(-1.1) LoRAs; Comfy-Org/ltx-2 Gemma encoders + ltx-2.3 rank-111 distilled repack; Kijai/LTX2.3_comfy splits | **LTX-2-Community-License** `[API-2026-09-15]` (repo LICENSE, dated 2026-01-05; Comfy-Org/Kijai carry the same terms on their conversions) | weights (fetch catalog, task 068xwy3 — the keep-utilities-only verdict's surviving LTX path) | community-license class: revenue-threshold commercial use + content-moderation + AI-disclosure duties; surfaced at consent time in the fetcher | surfaced |
| joyfox/LTX2.3-ICEdit-Insight (subtitles + watermark remove pair) | **Apache-2.0** `[API-2026-09-15]` (repo LICENSE) | weights (fetch catalog — the official remove templates' exact LoRAs) | notice; outputs remain subject to the LTX-2.3 base terms | clean |
| oumoumad/ltx-2.3-dearchive-lora, oumoumad/LTX-2.3-22b-IC-LoRA-Outpaint, WepeNerd/Obscura_Remova | `ltx-video-license` `[API-2026-09-15]` (LTX-2 derivatives; cards link the Lightricks LICENSE.txt) | weights (fetch catalog — restore-archival, outpaint, remove-object) | same community-license class as the base weights | surfaced |
| lllyasviel/Annotators (HED `ControlNetHED.pth`, MLSD `mlsd_large_512_fp32.pth`) | **NO-LICENSE** `[API-2026-09-14]` (repo carries only a `license: other` frontmatter tag, no license file) | weights (fetch-catalog preprocessors) | none triggered (we never redistribute; the user fetches their own copy with the license surfaced at consent) | fetchable-but-flagged |
| yzd-v/DWPose (ONNX pair) + hr16/DWPose-TorchScript-BatchSize5 | Apache-2.0 `[API-2026-09-14]` | weights (fetch-catalog preprocessors) | notice | clean |
| Comfy-Org/Depth-Anything-3 (Base) | Apache-2.0 `[API-2026-09-14]` | weights (fetch-catalog preprocessor, `geometry_estimation`) | notice | clean |
| smhfacct/Minimax-H3-fl2va-ref2va-hybrid-models (b25-49 int8) | inherits the MiniMax fl2va/ref2va terms (repo README) `[API-2026-09-14]` | weights (fetch-catalog OPTIONAL — runtime merge via the HybridLoader is preferred) | same class as the MiniMax base weights (§5 row 1); no additional grant | surfaced |
| jacokon/fasth3-live (W4A8 video VAE) | **minimax-h3-community-license-agreement** `[API-2026-09-15]` (dataset card gate text + LICENSE-MiniMax-H3.txt; the repo's CODE/prompts/characters are Apache-2.0 under its LICENSE — maintainer-verified at the 2026-09-15 assessment) | weights (fetch catalog, task gg7mu3s — VAE-side quantization candidate) | MiniMax H3 Community License: Applicable Territory worldwide EXCLUDING EU/UK/KR/US + commercial-use terms; the HF dataset carries a per-account license gate on top — surfaced at fetch consent, and anonymous fetches fail honestly (401). sha256 pin recovered via HF's own AV-scan VirusTotal reference (the gate masks the LFS oid for anonymous API reads); size pinned from the tree API | surfaced; prompt-pack import from this dataset is BLOCKED-EXTERNAL (gated content — needs maintainer-supplied files or a gate-accepted token) |
| MATLOWAI/minimax-h3-fused-turbo-int8-convrot | **minimax-h3-community-license-agreement** `[API-2026-09-15]` (repo LICENSE, license_name in card; folded lightx2v turbo + base conversion are Apache-2.0 sides, the merged weights stay MiniMax-H3 derivatives) | weights (fetch catalog, task gg7mu3s — the queued Ref2VA turbo bake-off fused arm, experiment-ladder task muwufpp) | same MiniMax H3 Community class (territory + commercial terms); author quality/timing claims unverified by us — surfaced at consent | surfaced |
| Kijai/MiniMax-H3-TAE (taeh3 preview decoder) | **Apache-2.0** `[API-2026-09-18]` (repo license tag; sha-pinned `a213ac8b`, file `vae_approx/taeh3.safetensors` 9,791,388 B, LFS sha256 `f0f60fa0…08e527`) | weights (fetch catalog `taeh3-preview-decoder`, task 2hbv2ib — F6 live previews: the H3 native sampler-preview decoder) | notice; preview-grade only (final frames always come from the real video VAE); a MiniMax-H3 derivative trained by Kijai for preview purposes | surfaced |
| ComfyUI reference checkout | GPL-3.0 (tag `v0.34.0`) | engine checkout (fetch catalog, §3 row) | the checkout is the user's own GPL working copy; we convey nothing | fetchable-but-flagged |

Node code that pairs with the Krea 2 LoRAs is permissive and already §4-clean:
`lbouaraba/comfyui-krea2edit` Apache-2.0, `alexw5702-afk/krea2-anypaint` MIT
(NOTICE credits Rebels + ostris), `ostris/ComfyUI-Krea2-Ostris-Edit` MIT,
`capitan01R/ComfyUI-Krea2T-Enhancer` MIT — all `[API-2026-09-14]`.

## 6. AGPLv3 compliance mechanics

- **LICENSE at repo root** — full AGPLv3 text `[LOCAL]`.
- **README → License** — license statement, fork-lineage pointer, source offer
  (§13), and a pointer to this inventory.
- **In-app notice** — Settings → "License & source" (always visible, not
  dismissible): app license + canonical source link. The one-time
  `LicenseNotice` component is a *different subject* — the MiniMax model
  license — and stays as is; two notices for two licenses, deliberately not
  conflated.
- **§13 network interaction** — the server serves the web app over HTTP, so
  remote users are network users. Since the app is distributed as source and
  run by its own operator, the Corresponding Source for an unmodified
  deployment is the repository itself; operators of modified copies point
  theirs at their fork (README + Settings carry the canonical URL). There is
  no binary distribution today, so no separate written-offer machinery is
  needed.
- **Upstream ARR conflict** — the fork-lineage position (machine-generated
  upstream, independent rewrite) is recorded in
  [PROVENANCE.md](PROVENANCE.md) with its IANAL caveat; nothing mechanical to
  add. If upstream ever asserts, the recorded resolution path is completion of
  the rewrite.
- **Third-party code inside the app** — vendored packs ship their LICENSE
  files (audited); dependencies ship notices via manifests (§1).

## 7. Headers policy — recommendation (maintainer to decide)

Per-file headers were **deliberately deferred** when AGPLv3 was adopted; this
section is the promised options memo. Default until the maintainer picks:
**keep the current no-headers stance.**

| Option | What | Strengths | Costs |
| --- | --- | --- | --- |
| **A. Status quo** (recommended default) | root LICENSE + README + Settings notice + this inventory + `pnpm license:audit` as the SPDX manifest | zero friction; how most of the npm ecosystem ships | a file copied out of the repo carries no license line of its own |
| **B. SPDX line per file** | `// SPDX-License-Identifier: AGPL-3.0-or-later` as line 1 of every source file | machine-checkable (reuse.software tooling); travels with every file; one line, no boilerhead | one codemod + a lint rule to keep it true on new files |
| **C. Full GPL-style header blocks** | traditional license + copyright header per file | maximal traditional signaling | noisy, merge-churn, redundant with the root license |
| **D. NOTICE file** | root `NOTICE` consolidating third-party attributions (Apache §4 hygiene) | single place auditors read; future-proofs a bundled dist | another file to keep in sync (could be emitted by the audit script) |

If Option B is chosen later, the codemod is ~20 lines and the audit script
grows a check for it. If a bundled/binary distribution ever ships, do D + a
generated notices bundle first (§9.2).

*Post-rewrite cross-check (2026-09-17, task 68rnn84): still accurate — the
canvas rewrite landed with no per-file headers (457 tracked files at HEAD);
default A remains in force; the decision stays staged with the maintainer.*

## 8. VDN LongCache patch — distribution analysis (flagged for maintainer sign-off)

**The question.** `server/enginePatch.ts` ships a patcher whose content
constants (`RUN_BLOCKS`, `HOOK_LOOP`) are a verbatim port of the maintainer's
own fork installer (ComfyUI-VDN-H3-24GB, Apache-2.0), applied at runtime —
with recorded consent — against `comfy/ldm/minimax/model.py` in **ComfyUI, a
GPL-3.0 program**. Does shipping this create obligations for our AGPLv3
codebase?

**Analysis.**

1. *The tool vs the patched program.* GPL obligations attach to
   **conveying** copies. We convey the patcher; we never convey ComfyUI, a
   pre-patched ComfyUI file, or even a patch/diff carrying ComfyUI context
   lines. The modified `model.py` exists only on the user's machine, produced
   by the user's consented action on the user's own checkout — GPL §0/§9
   leave modification of one's own copy unrestricted. This is the
   ComfyUI-Manager-class pattern the ecosystem treats as the sanctioned way
   to touch core.
2. *Is the patcher a derivative of ComfyUI?* The layout regexes encode
   structural facts (a call sequence), not literal expression. The constants
   are literal text — the honest crux, with two readings:
   - **(a) Apache-2.0 reading:** the fork's author re-implemented the loop and
     licensed it Apache-2.0 → our port is plain Apache-2.0 inclusion; §4
     duties (retain notices, state changes) are met by the in-file attribution
     and this record.
   - **(b) GPL-3.0 reading:** `RUN_BLOCKS` "mirrors the upstream loop exactly
     plus slicing" — if it is substantially copied from ComfyUI's own loop,
     the fork's Apache-2.0 grant is ineffective for that copied portion (a
     licensee cannot relicense the licensor's code) and those ~45 lines remain
     GPL-3.0 in origin. GPL-3.0 and AGPLv3 are explicitly
     combining-compatible (GPLv3 §13, final paragraph) and our app already
     accepts §13 network terms globally, so the repo remains distributable
     under AGPLv3 with those lines carrying GPL attribution duties (credit
     ComfyUI for the loop text).
   Either reading yields the same conclusion: **no blocker, only attribution.**
3. *Residual duties we already meet:* never distribute a pre-applied patch
   (the patcher is the only distribution form — keep it that way); the consent
   gate + pristine-backup revert keep the user's modified copy inspectable and
   reversible; attribution records live in `server/enginePatch.ts`'s header
   and here.

**Verdict:** no obligations are created for the AGPLv3 codebase beyond
attribution and the never-distribute-pre-applied rule we already follow.

**Options** (legal-adjacent judgment — maintainer sign-off requested; the
PROVENANCE IANAL stance continues):

1. **Status quo + documentation (recommended):** patcher ships as code,
   applies user-locally only, consent-gated, reversible; attribution as
   recorded. Cost: none. Risk: reading (b) stays theoretically open — and is
   harmless per §8.2.
2. **Independent re-implementation** of the constants from ComfyUI's published
   behavior (not porting the fork's text): removes reading (b) entirely;
   costs re-validation of the patched layouts.
3. **Upstream the `("block_loop", 0)` extension point to ComfyUI core:** kills
   the entire question on any version that has it (our version gate then
   degrades the patcher to unused). Best long-term engineering outcome too.

*Post-rewrite cross-check (2026-09-17, task 68rnn84): analysis unchanged and
still accurate — `server/enginePatch.ts` survived the canvas rewrite intact
(patch constants and attribution header verified present in the tree); the
verdict and the option pick remain staged with the maintainer.*

## 9. Open items / follow-ups

1. ~~**facok pin is a moving target**~~ [Resolved 2026-09-14, task hgjbea2]:
   the consent-gated fetcher resolves branch pins to the HEAD SHA at fetch
   time and stamps that SHA into the install marker and the fetch install
   record (`server/fetcher.ts` pin discipline; tested in
   `tests/fetcher.test.js` §d). Local-directory installs of a
   branch-pinned pack still inherit the registry string — the branch flows
   only through the fetcher.
2. **Bundled-distribution notices** — if a minified/binary dist ever ships,
   generate third-party license notices from `node_modules` (extend
   `scripts/audit-licenses.cjs`, or a `vite-plugin-license`-class step) and
   add a NOTICE file (§7 option D).
3. **LTX-2.5 / ACE-Step 1.5 / Z-Image weight licenses** — record SPDX + duties
   rows in §5 when those workspaces go first-class (currently `[DOC]`-free).
4. **thedoxen/Krea-2-pose-controlnet** — verify the Apache-2.0 claim at
   adoption time (HF shows no license tag today).
5. **Upstreaming the block-loop hook** — §8 option 3; also removes the
   version-gate maintenance burden.
6. **§1 table regeneration (drift found 2026-09-17, task 68rnn84)** — the §1
   dependency table is stale: 28 rows recorded vs 37 direct deps in
   `package.json` (nine added after the 2026-09-14 audit:
   `@huggingface/transformers` Apache-2.0; `d3-selection` / `d3-transition` /
   `d3-zoom` ISC; `hash-wasm` and `react-rnd` MIT; plus the three MIT
   `@types/d3-*` stubs — each verified against the npm registry on
   2026-09-17; all permissive, so the §1 conclusion holds). Paste the
   `pnpm license:audit` output back into §1 at the next dependency-touching
   change, and consider making CI diff the table against the live manifest
   so this cannot drift silently again.
   *[Partially resolved 2026-09-21, task nwtoz6y: the audit's registry-lockstep
   check now names-checks every live dep against `docs/licenses/registry.md`
   (the missing `vitest` row it immediately caught is above), and the deep
   per-component rows live in [licenses/registry.md](licenses/registry.md).
   The §1 table itself still regenerates by paste — the lockstep check bounds
   the drift to versions, not presence.]*

The fetch catalog (`server/fetchCatalog.ts`, task hgjbea2) pins sizes and
sha256 digests for its weight entries, verified against the HF API on
2026-09-14 (`[API-2026-09-14]`, `x-linked-etag` for LFS files) and on
2026-09-15 for the fasth3-live rows (task gg7mu3s): the gated dataset's
sizes come from the tree API, and the w4a8 VAE's sha256 was recovered
through HF's own AV-scan VirusTotal reference (the license gate masks the
LFS oid for anonymous reads — the hash is HF-computed from the actual file
bytes; method recorded in the catalog header and §5). Its license
verdicts are transcribed from this inventory (and single-sourced from
`ENGINE_NODE_PACKS` for the pack rows — the fetcher suite's integrity
section fails the build if a catalog row ships without a license verdict or
drifts from the pack registry).
